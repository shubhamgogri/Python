# Python
Create Your own Dictionary using Python with prediction of words that are incorrectly spelled.
The data is in Json file which is used to display the words in the dictionary by using json package.
The difflib package is used to predict the words or rather getting the closely matched words from the input.
